meta {
  name: Add Participant
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/room-participants
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body {
  json {
    room_id: "{{uuid}}"
    user_id: "{{uuid}}"
  }
}
